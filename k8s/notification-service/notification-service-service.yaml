apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: default
  labels:
    app: notification-service
spec:
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  selector:
    app: notification-service
  type: ClusterIP
